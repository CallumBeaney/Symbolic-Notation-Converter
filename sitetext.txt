EXAMPLE:

    ALL n IN Evens. EXISTS p, q IN Primes. n EQUALS p ADD q
                            ↓
            ∀n ∈ Evens. ∃p, q ∈ Primes. n = p + q. 


DICTIONARY:

    ALL/EVERY   ∀
    IN          ∈
    EXIST/S     ∃
    EQUALS      =
    ADD/PLUS    +


ABOUT:

    This program was conceived by Callum Beaney and Jim Miles for quick conversion from verbose input to symbolic notation.
